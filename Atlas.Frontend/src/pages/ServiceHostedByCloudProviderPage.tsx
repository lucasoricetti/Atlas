import createRelationRoutePage from "./createRelationRoutePage";

export default createRelationRoutePage({
  title: "Cloud Providers that host this Service",
  sourceType: "service",
  relationType: "hosted-by-cloudprovider"
});
