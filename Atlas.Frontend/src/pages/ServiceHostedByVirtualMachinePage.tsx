import createRelationRoutePage from "./createRelationRoutePage";

export default createRelationRoutePage({
  title: "Virtual Machines that host this Service",
  sourceType: "service",
  relationType: "hosted-by-virtualmachine"
});
